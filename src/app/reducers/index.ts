export interface RootState {
  // todos: RootState.TodoState; // Example of a simple state slice
  router?: any;
}

export namespace RootState {
  // export type TodoState = TodoModel[]; // Example of a simple state slice type
}
